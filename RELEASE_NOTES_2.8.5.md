## 电池内显 2.8.5

- 修复部分 Mac 重启或重新登录后自动弹出设置窗口的问题
- 登录启动现在保持完整静默，只创建菜单栏电池指示器
- 忽略 macOS 在应用启动阶段发送的合成 reopen 事件
- 登录完成后，用户从“应用程序”再次打开“电池内显”仍会正常显示设置窗口
- 保持原有 `SMAppService.mainAppService` 登录项、菜单栏位置记忆和电池显示逻辑不变
- 继续支持 Intel (`x86_64`) 与 Apple 芯片 (`arm64`)
